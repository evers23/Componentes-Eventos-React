export default function SocialButton({ icon }) {
  return (
          <button className="btn"
                          >
              <i className={`fa fa-${icon}`} /> {icon}
          </button>
  );
}
  
  
  
  
  
  
  